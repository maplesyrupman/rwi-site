type Props = {
    year?: boolean,
    month?: boolean,
    day?: boolean,
    change: () => void
}

export default function Date({year, month, day, change}: Props) {

    return (
        <div>
            
        </div>
    )
}